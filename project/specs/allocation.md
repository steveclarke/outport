# Allocation

> How ports get assigned, the hashing algorithm, registry behavior, and conflict resolution.

## Status: Placeholder

To be written. See `internal/allocator/` and `internal/registry/` for current implementation.
